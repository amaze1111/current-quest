# Render (and other hosts) set NODE_ENV=production before running the build command, which
# makes npm silently skip devDependencies — including typescript and @types/* that `npm run
# build` actually needs. This overrides that so `npm ci` always installs everything; the build
# output itself doesn't include devDependencies regardless.
production=false
